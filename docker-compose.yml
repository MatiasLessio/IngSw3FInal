version: '3'

services:
  backend:
    container_name: backend
    build: ./backend
    restart: on-failure
    ports:
      - "8090:8090"
  frontend:
    container_name: frontend
    build: ./frontend
    ports:
      - "4200:4200"
  depends_on:
    backend:
      condition: service_started
